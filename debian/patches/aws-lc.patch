From: Ubuntu Developers <ubuntu-devel-discuss@lists.ubuntu.com>
Date: Wed, 28 Jan 2026 17:03:54 +0100
Subject: aws-lc-sys: advertise embedded static library to dh-cargo

---
 rust-vendor/aws-lc-sys/builder/main.rs | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/rust-vendor/aws-lc-sys/builder/main.rs b/rust-vendor/aws-lc-sys/builder/main.rs
index 07a03e7..084151c 100644
--- a/rust-vendor/aws-lc-sys/builder/main.rs
+++ b/rust-vendor/aws-lc-sys/builder/main.rs
@@ -1369,6 +1369,10 @@ pub(crate) fn emit_source_build_metadata(manifest_dir: &Path) {
     }
 
     println!("cargo:rerun-if-changed=aws-lc/");
+    let version = env::var("AWS_LC_SYS_VERSION")
+    .expect("AWS_LC_SYS_VERSION must be set")
+    .replace('.', "_");
+    println!("dh-cargo:deb-built-using=aws_lc_{}_crypto=0={}", version, env::var("CARGO_MANIFEST_DIR").unwrap());
 }
 
 fn setup_include_paths(out_dir: &Path, manifest_dir: &Path) -> PathBuf {
